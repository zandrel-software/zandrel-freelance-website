<header class="site-header">
    <div class="container site-header__inner">
        <a class="site-header__brand"
           href="#inicio"
           aria-label="Zandrel - Inicio"
           @onclick="CloseMenu">
            <img src="images/brand/zandrel-logo-horizontal-transparent.png"
                 alt="Zandrel"
                 class="site-header__logo" />
        </a>

        <button class="site-header__menu-toggle @(_isMenuOpen ? "site-header__menu-toggle--open" : string.Empty)"
                type="button"
                aria-label="@(_isMenuOpen ? "Cerrar menú" : "Abrir menú")"
                aria-controls="site-navigation"
                aria-expanded="@(_isMenuOpen ? "true" : "false")"
                @onclick="ToggleMenu">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <nav id="site-navigation"
             class="@($"site-header__navigation {(_isMenuOpen ? "site-header__navigation--open" : string.Empty)}")"
             aria-label="Navegación principal">

            <a href="#inicio"
               class="site-header__link"
               @onclick="CloseMenu">
                Inicio
            </a>

            <a href="#proyectos"
               class="site-header__link"
               @onclick="CloseMenu">
                Proyectos
            </a>

            <a href="#proceso"
               class="site-header__link"
               @onclick="CloseMenu">
                Cómo trabajamos
            </a>

            <a href="#contacto"
               class="site-header__link"
               @onclick="CloseMenu">
                Contacto
            </a>

            <a href="https://wa.me/527751272647?text=Hola%2C%20vi%20la%20p%C3%A1gina%20de%20Zandrel%20y%20me%20gustar%C3%ADa%20platicar%20sobre%20un%20proyecto."
               class="button button--primary site-header__cta"
               target="_blank"
               rel="noopener noreferrer"
               aria-label="Cuéntanos tu proyecto por WhatsApp"
               @onclick="CloseMenu">
                Cuéntanos tu proyecto
            </a>
        </nav>
    </div>
</header>

@code {
    private bool _isMenuOpen;

    private void ToggleMenu()
    {
        _isMenuOpen = !_isMenuOpen;
    }

    private void CloseMenu()
    {
        _isMenuOpen = false;
    }
}